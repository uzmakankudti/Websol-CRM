{
  "name": "websol-crm-backend",
  "version": "0.1.0",
  "description": "Websol CRM backend — Azure Functions (serverless), Node + TypeScript.",
  "main": "dist/src/index.js",
  "engines": {
    "node": ">=20 <23"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "seed": "DB_HOST=localhost DB_USER=websol DB_PASSWORD=Websol@Local123 DB_NAME=websol_crm npx tsx src/seed.ts",
    "check": "npm run lint && npm run test"
  },
  "dependencies": {
    "@azure/functions": "^4.5.0",
    "mysql2": "^3.11.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "eslint": "^8.57.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
